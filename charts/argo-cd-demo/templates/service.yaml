apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-cd-demo.fullname" . }}
  labels:
    {{- include "argo-cd-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argo-cd-demo.selectorLabels" . | nindent 4 }}
